# RC.3 Full Sync

Full EVENTO v0.9.0-rc.3 source synchronization is in progress. The next Android build must preserve package identity and the persistent development signing certificate so it installs as an update over the already verified phone build.
