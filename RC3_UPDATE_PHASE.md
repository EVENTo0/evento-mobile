# RC.3 Phone Update Phase

The full customer application source has been synchronized in stages. The remaining gate is to switch the entrypoint and tests, let GitHub Actions build the next APK with the same application ID and development signing certificate, and verify that Android installs it as an update over the already installed bootstrap build.
