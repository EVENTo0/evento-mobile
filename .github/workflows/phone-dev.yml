name: EVENTO Phone Dev APK (Legacy - Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for history only. Use phone-dev-fixed.yml."
